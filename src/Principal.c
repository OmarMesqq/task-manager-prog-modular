#include "Principal.h"

/**
 * Ponto de entrada do aplicativo. Inicializa o contexto e demais alocações
 * @returns `NULL` em caso de erro ou `Principal*` em caso de sucesso
 */
Principal* principal_iniciar(void) {

}